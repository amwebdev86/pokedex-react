import { useEffect, useState } from 'react';
import './App.css';

function App({ login }) {
  const [data, setData] = useState(null);

  useEffect(() => {
    
  })
  return (
    <div className="App">
     <h1>Pokedex</h1>
    </div>
  );
}

export default App;
